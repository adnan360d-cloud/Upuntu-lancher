name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # 1. فك الضغط أولاً قبل إعداد الجافا لتكون الملفات مرئية للنظام
    - name: Unzip Project Files
      run: |
        unzip ubuntu-touch-launcher.zip -d extracted_project
        if [ -d "extracted_project/upuntu tutch" ]; then
          mv "extracted_project/upuntu tutch/"* .
        elif [ -d "extracted_project/ubuntu-touch-launcher" ]; then
          mv "extracted_project/ubuntu-touch-launcher/"* .
        else
          mv extracted_project/* .
        fi

    # 2. إعداد الجافا بدون خاصية الكاش لتجنب خطأ الفحص المسبق
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    # 3. التأكد من وجود ملف تشغيل Gradle وصلاحياته
    - name: Grant execute permission for gradlew
      run: |
        if [ ! -f "gradlew" ]; then
          gradle wrapper
        fi
        chmod +x gradlew

    # 4. بناء التطبيق
    - name: Build with Gradle
      run: ./gradlew assembleDebug

    # 5. رفع ملف الـ APK النهائي للتحميل
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
        
