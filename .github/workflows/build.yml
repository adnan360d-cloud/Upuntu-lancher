name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # 1. فك ضغط ملفات المشروع
    - name: Unzip Project Files
      run: |
        unzip ubuntu-touch-launcher.zip -d extracted_project
        if [ -d "extracted_project/upuntu tutch" ]; then
          mv "extracted_project/upuntu tutch/"* .
        elif [ -d "extracted_project/ubuntu-touch-launcher" ]; then
          mv "extracted_project/ubuntu-touch-launcher/"* .
        else
          mv extracted_project/* .
        fi

    # 2. إنشاء ملف بيئة وهمي لتخطي الأخطاء
    - name: Create Mock Environment File
      run: |
        touch .env.example
        touch .env

    # 3. تخطي التوقيع المخصص وإجبار النظام على استخدام التوقيع الافتراضي لأندرويد
    - name: Bypass Custom Code Signing
      run: |
        if [ -f "app/build.gradle.kts" ]; then
          sed -i 's/.*signingConfig =.*/\/\/ bypassed custom signing/g' app/build.gradle.kts
        fi
        if [ -f "app/build.gradle" ]; then
          sed -i 's/.*signingConfig .*/\/\/ bypassed custom signing/g' app/build.gradle
        fi

    # 4. تعديل الإعدادات لتعطيل الكاش الرسومي المسبب للانهيار
    - name: Fix Gradle Properties for Headless CI
      run: |
        echo "org.gradle.configuration-cache=false" >> gradle.properties
        echo "kotlin.compiler.execution.strategy=daemon" >> gradle.properties

    # 5. إعداد الجافا المتوافقة (إصدار 21)
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    # 6. توليد واستخدام إصدار Gradle المتوافق المطلوب (9.3.1)
    - name: Setup Gradle Wrapper
      run: |
        gradle wrapper --gradle-version 9.3.1
        chmod +x gradlew

    # 7. بناء التطبيق النهائي
    - name: Build with Gradle
      run: ./gradlew assembleDebug --no-daemon

    # 8. رفع ملف الـ APK النهائي للتحميل
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
        
